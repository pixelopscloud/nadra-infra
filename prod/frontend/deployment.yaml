apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: nadra-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: nadraacr.azurecr.io/nadra-frontend:latest
        ports:
        - containerPort: 80
