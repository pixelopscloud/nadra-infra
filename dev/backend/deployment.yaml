apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: nadra-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: nadraacr.azurecr.io/nadra-backend:c456f0db997596fd628f107eaa9382c8235cda4c
        env:
        - name: DB_HOST
          value: postgres
        - name: DB_USER
          value: nadra
        - name: DB_PASSWORD
          value: nadra123
        - name: DB_NAME
          value: nadradb
        ports:
        - containerPort: 3000
